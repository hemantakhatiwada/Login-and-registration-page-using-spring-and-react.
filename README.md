Setup Instructions

1. Import database_schema.sql into MySQL
2. Create application.properties in backend/resources:
3.create a table inside mysql from database schema
  
3. Run backend (Spring Boot)
4. Run frontend (npm run dev)

5. (Make sure to check properties, cross axiom, and match the localhost of db,backend and frontend.)
